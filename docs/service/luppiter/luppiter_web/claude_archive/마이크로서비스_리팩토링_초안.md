# Luppiter Web 마이크로서비스 리팩토링 초안

## 📋 목차
1. [현재 상태 분석](#현재-상태-분석)
2. [리팩토링 목표 및 원칙](#리팩토링-목표-및-원칙)
3. [도메인 분리 전략](#도메인-분리-전략)
4. [구체적 리팩토링 방안](#구체적-리팩토링-방안)
5. [점진적 마이그레이션 전략](#점진적-마이그레이션-전략)
6. [운영 리스크 최소화 방안](#운영-리스크-최소화-방안)
7. [구현 우선순위](#구현-우선순위)

---

## 현재 상태 분석

### 1.1 아키텍처 현황
- **프레임워크**: Spring Boot 2.7.5 + MyBatis
- **데이터베이스**: PostgreSQL
- **구조**: 모놀리식 애플리케이션 (WAR 패키징)
- **Mapper 구조**: 8개의 mapper 패키지로 분산 (mapper, mapper2~mapper8)

### 1.2 주요 문제점

#### 1.2.1 복잡한 쿼리 구조
**예시: `selectSpecificZoneInfoList` (sql-dashboard.xml:115-145)**
```sql
-- 단일 쿼리에서 수행하는 작업들:
1. 인벤토리 마스터 데이터 조회 (inventory_master, inventory_master_sub)
2. 사용자 권한 필터링 (cmon_layer_code_info, cmon_group_layer, cmon_group_user)
3. 존 타입 매핑 조회 (c02_zone_type_mapping)
4. 공통코드 조회 (c00_common_code - IDC, Zone Type)
5. 데이터 집계 및 그룹핑
```

**문제점:**
- 단일 책임 원칙(SRP) 위반: 하나의 쿼리가 여러 도메인 로직을 포함
- 재사용성 저하: 다른 서비스에서 부분 데이터가 필요해도 전체 쿼리 실행
- 테스트 어려움: 복잡한 JOIN으로 인한 단위 테스트 복잡도 증가
- 성능 이슈: 불필요한 데이터 조인으로 인한 성능 저하 가능성
- 유지보수 어려움: 비즈니스 로직 변경 시 쿼리 전체 수정 필요

#### 1.2.2 Mapper 구조의 문제
- **도메인별 분리 부족**: 기능별이 아닌 패키지 번호로만 분리 (mapper, mapper2~mapper8)
- **책임 혼재**: DashboardMapper에 이벤트, 인벤토리, 존 매핑, 공통코드 등이 혼재
- **의존성 복잡도**: Service 레이어에서 단일 Mapper에 과도하게 의존

#### 1.2.3 Service 레이어의 역할 부족
- **데이터 변환 중심**: Service가 단순히 Mapper 결과를 변환하는 역할만 수행
- **비즈니스 로직 부재**: 실제 비즈니스 로직이 쿼리 내부에 포함
- **조합 로직 부족**: 여러 도메인 데이터를 조합하는 로직이 Service에 없음

---

## 리팩토링 목표 및 원칙

### 2.1 목표
1. **도메인별 책임 분리**: 각 Mapper는 단일 도메인만 담당
2. **Service 레이어 강화**: 비즈니스 로직과 데이터 조합을 Service에서 처리
3. **재사용성 향상**: 작은 단위의 쿼리로 분리하여 재사용 가능
4. **테스트 용이성**: 단위 테스트 작성이 쉬운 구조
5. **운영 안정성**: 기존 기능 영향 최소화하며 점진적 개선

### 2.2 원칙
- **점진적 리팩토링**: Big Bang 방식 금지, 단계적 마이그레이션
- **하위 호환성 유지**: 기존 API 인터페이스 유지
- **이중화 전략**: 신규 구조와 기존 구조 병행 운영 후 전환
- **모니터링 강화**: 변경 사항에 대한 성능 및 오류 모니터링
- **롤백 계획**: 문제 발생 시 즉시 롤백 가능한 구조

---

## 도메인 분리 전략

### 3.1 도메인 식별

#### 3.1.1 핵심 도메인
1. **Event Domain** (이벤트)
   - 책임: 이벤트 정보 조회, 이벤트 상태 관리
   - 관련 테이블: `cmon_event_info`, `cmon_event_exception`, `cmon_event_group`
   - Mapper: `EventMapper` (신규 생성)

2. **Inventory Domain** (인벤토리)
   - 책임: 장비 정보 조회, 인벤토리 관리
   - 관련 테이블: `inventory_master`, `inventory_master_sub`, `auto_inventory`
   - Mapper: `InventoryMapper` (기존 `InventoryManageMapper` 확장)

3. **Zone Domain** (존 관리)
   - 책임: 존 매핑 정보, 존 타입 관리
   - 관련 테이블: `c02_zone_type_mapping`, `c00_common_code` (ZONE_TYPE)
   - Mapper: `ZoneMapper` (신규 생성)

4. **User/Authorization Domain** (사용자/권한)
   - 책임: 사용자 권한 조회, 그룹별 접근 제어
   - 관련 테이블: `cmon_user`, `cmon_group_user`, `cmon_group_layer`, `cmon_layer_code_info`
   - Mapper: `UserAuthorizationMapper` (신규 생성)

5. **CommonCode Domain** (공통코드)
   - 책임: 공통코드 조회
   - 관련 테이블: `c00_common_code`
   - Mapper: `CommonCodeMapper` (기존 `CommonCodeMapper` 활용)

6. **Incident Domain** (인시던트)
   - 책임: 인시던트 정보 조회
   - 관련 테이블: `cmon_incident_info`, `cmon_incident_event_info`
   - Mapper: `IncidentMapper` (기존 `IcdMapper` 확장)

7. **Dashboard Domain** (대시보드)
   - 책임: 대시보드 데이터 조합 및 집계
   - Mapper: `DashboardMapper` (기존 유지, 역할 축소)

### 3.2 도메인 간 의존성

```
Dashboard Domain
    ↓ (의존)
    ├── Event Domain
    ├── Inventory Domain
    ├── Zone Domain
    ├── User/Authorization Domain
    ├── CommonCode Domain
    └── Incident Domain
```

**원칙**: Dashboard는 다른 도메인들을 조합만 하고, 직접 쿼리하지 않음

---

## 구체적 리팩토링 방안

### 4.1 예시: `selectSpecificZoneInfoList` 리팩토링

#### 4.1.1 현재 구조 분석

**현재 쿼리 (sql-dashboard.xml:115-145)**
```sql
SELECT
    zt.code AS zone_type_code
    , zt.code_nm AS zone_type_name
    , idc.code AS center_code
    , idc.code_nm AS center_name
    , ztm.l4_code AS zone_code
    , ztm.l4_code AS zone_name
    , count(1) as device_count
FROM inventory_master mst
    LEFT JOIN inventory_master_sub sub ON mst.zabbix_ip = sub.zabbix_ip
    INNER JOIN (
        -- 사용자 권한 필터링 서브쿼리
        SELECT inv.zabbix_ip
        FROM inventory_master inv
            LEFT JOIN inventory_master_sub sub ON inv.zabbix_ip = sub.zabbix_ip 
            INNER JOIN cmon_layer_code_info lci ON ...
            INNER JOIN cmon_group_layer gl ON ...
            INNER JOIN cmon_group_user gu ON ... AND gu.user_id = #{user_id}
        GROUP BY inv.zabbix_ip
    ) AS inv ON mst.zabbix_ip = inv.zabbix_ip
    INNER JOIN c00_common_code idc ON ...
    INNER JOIN c02_zone_type_mapping ztm ON ...
    LEFT JOIN c00_common_code zt ON ...
```

**문제점:**
- 인벤토리 조회 + 사용자 권한 필터링 + 존 매핑 + 공통코드 조회가 하나의 쿼리에 혼재

#### 4.1.2 리팩토링 후 구조

**1단계: 도메인별 Mapper 분리**

**UserAuthorizationMapper.java** (신규)
```java
@Mapper
public interface UserAuthorizationMapper {
    /**
     * 사용자 권한에 따른 접근 가능한 zabbix_ip 목록 조회
     */
    List<String> selectAuthorizedZabbixIpList(@Param("userId") String userId);
}
```

**sql-user-authorization.xml** (신규)
```xml
<select id="selectAuthorizedZabbixIpList" resultType="String">
    SELECT DISTINCT inv.zabbix_ip
    FROM inventory_master inv
        LEFT JOIN inventory_master_sub sub ON inv.zabbix_ip = sub.zabbix_ip 
        INNER JOIN cmon_layer_code_info lci 
            ON COALESCE(sub.host_group_nm, inv.host_group_nm) = lci.layer_nm 
            AND lci.delete_yn = 'N'
        INNER JOIN cmon_group_layer gl ON gl.layer_cd = lci.layer_cd
        INNER JOIN cmon_group_user gu 
            ON gu.group_id = gl.group_id 
            AND gu.delete_yn = 'N' 
            AND gu.user_id = #{userId}
</select>
```

**InventoryMapper.java** (확장)
```java
@Mapper
public interface InventoryMapper {
    /**
     * zabbix_ip 목록에 해당하는 인벤토리 기본 정보 조회
     */
    List<InventoryInfo> selectInventoryInfoByZabbixIpList(
        @Param("zabbixIpList") List<String> zabbixIpList
    );
}
```

**sql-inventory.xml** (추가)
```xml
<select id="selectInventoryInfoByZabbixIpList" resultType="com.framework.domain.InventoryInfo">
    SELECT
        mst.zabbix_ip
        , mst.zone
        , mst.idc_center_code
        , COALESCE(sub.host_group_nm, mst.host_group_nm) AS host_group_nm
    FROM inventory_master mst
        LEFT JOIN inventory_master_sub sub ON mst.zabbix_ip = sub.zabbix_ip
    WHERE mst.zabbix_ip IN
        <foreach collection="zabbixIpList" item="ip" open="(" separator="," close=")">
            #{ip}
        </foreach>
</select>
```

**ZoneMapper.java** (신규)
```java
@Mapper
public interface ZoneMapper {
    /**
     * L4 코드로 존 타입 매핑 정보 조회
     */
    ZoneTypeMapping selectZoneTypeMappingByL4Code(@Param("l4Code") String l4Code);
    
    /**
     * L4 코드 목록으로 존 타입 매핑 정보 일괄 조회
     */
    List<ZoneTypeMapping> selectZoneTypeMappingByL4CodeList(
        @Param("l4CodeList") List<String> l4CodeList
    );
}
```

**sql-zone.xml** (신규)
```xml
<select id="selectZoneTypeMappingByL4CodeList" resultType="com.framework.domain.ZoneTypeMapping">
    SELECT
        l4_code AS zoneCode
        , zone_type AS zoneTypeCode
    FROM c02_zone_type_mapping
    WHERE l4_code IN
        <foreach collection="l4CodeList" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
</select>
```

**2단계: Service 레이어에서 조합**

**DashboardServiceImpl.java** (수정)
```java
@Service
public class DashboardServiceImpl implements DashboardService {
    
    @Autowired
    private UserAuthorizationMapper userAuthorizationMapper;
    
    @Autowired
    private InventoryMapper inventoryMapper;
    
    @Autowired
    private ZoneMapper zoneMapper;
    
    @Autowired
    private CommonCodeService commonCodeService;
    
    @Override
    public List<Map<String, Object>> getSpecificZoneInfoList(Map<String, Object> params) throws Exception {
        String userId = (String) params.get("user_id");
        
        // 1. 사용자 권한에 따른 접근 가능한 zabbix_ip 목록 조회
        List<String> authorizedZabbixIpList = 
            userAuthorizationMapper.selectAuthorizedZabbixIpList(userId);
        
        if (authorizedZabbixIpList.isEmpty()) {
            return new ArrayList<>();
        }
        
        // 2. 인벤토리 기본 정보 조회
        List<InventoryInfo> inventoryList = 
            inventoryMapper.selectInventoryInfoByZabbixIpList(authorizedZabbixIpList);
        
        // 3. 존 코드 추출
        List<String> zoneCodeList = inventoryList.stream()
            .map(InventoryInfo::getZone)
            .distinct()
            .collect(Collectors.toList());
        
        // 4. 존 타입 매핑 정보 조회
        List<ZoneTypeMapping> zoneMappingList = 
            zoneMapper.selectZoneTypeMappingByL4CodeList(zoneCodeList);
        
        // 5. 공통코드 조회 (캐싱 가능)
        List<Map<String, Object>> zoneTypeList = 
            commonCodeService.getCommonCodeList("ZONE_TYPE");
        List<Map<String, Object>> centerCodeList = 
            commonCodeService.getCommonCodeList("IDC_CENTER_CODE");
        
        // 6. Service 레이어에서 데이터 조합
        return combineZoneInfo(
            inventoryList, 
            zoneMappingList, 
            zoneTypeList, 
            centerCodeList
        );
    }
    
    /**
     * 인벤토리, 존 매핑, 공통코드 데이터를 조합하여 최종 결과 생성
     */
    private List<Map<String, Object>> combineZoneInfo(
            List<InventoryInfo> inventoryList,
            List<ZoneTypeMapping> zoneMappingList,
            List<Map<String, Object>> zoneTypeList,
            List<Map<String, Object>> centerCodeList) {
        
        // 존 코드별로 그룹핑
        Map<String, List<InventoryInfo>> inventoryByZone = inventoryList.stream()
            .collect(Collectors.groupingBy(InventoryInfo::getZone));
        
        // 존 타입 매핑 Map 생성
        Map<String, ZoneTypeMapping> zoneMappingMap = zoneMappingList.stream()
            .collect(Collectors.toMap(
                ZoneTypeMapping::getZoneCode,
                Function.identity()
            ));
        
        // 공통코드 Map 생성
        Map<String, String> zoneTypeNameMap = zoneTypeList.stream()
            .collect(Collectors.toMap(
                m -> (String) m.get("code"),
                m -> (String) m.get("code_nm")
            ));
        
        Map<String, String> centerNameMap = centerCodeList.stream()
            .collect(Collectors.toMap(
                m -> (String) m.get("code"),
                m -> (String) m.get("code_nm")
            ));
        
        // 최종 결과 생성
        List<Map<String, Object>> result = new ArrayList<>();
        
        for (Map.Entry<String, List<InventoryInfo>> entry : inventoryByZone.entrySet()) {
            String zoneCode = entry.getKey();
            List<InventoryInfo> inventories = entry.getValue();
            
            ZoneTypeMapping mapping = zoneMappingMap.get(zoneCode);
            if (mapping == null) continue;
            
            // IDC 센터별로 그룹핑
            Map<String, Long> deviceCountByCenter = inventories.stream()
                .collect(Collectors.groupingBy(
                    InventoryInfo::getIdcCenterCode,
                    Collectors.counting()
                ));
            
            for (Map.Entry<String, Long> centerEntry : deviceCountByCenter.entrySet()) {
                Map<String, Object> item = new HashMap<>();
                item.put("zone_type_code", mapping.getZoneTypeCode());
                item.put("zone_type_name", zoneTypeNameMap.get(mapping.getZoneTypeCode()));
                item.put("center_code", centerEntry.getKey());
                item.put("center_name", centerNameMap.get(centerEntry.getKey()));
                item.put("zone_code", zoneCode);
                item.put("zone_name", zoneCode);
                item.put("device_count", centerEntry.getValue());
                
                result.add(item);
            }
        }
        
        return result;
    }
}
```

#### 4.1.3 리팩토링 효과

**장점:**
1. **재사용성 향상**: 각 Mapper 메서드를 다른 곳에서도 활용 가능
2. **테스트 용이성**: 각 도메인별로 독립적인 단위 테스트 작성 가능
3. **성능 최적화**: 필요한 데이터만 조회, 캐싱 적용 용이
4. **유지보수성**: 도메인별 변경 시 해당 Mapper만 수정
5. **확장성**: 새로운 도메인 추가 시 기존 코드 영향 최소화

**주의사항:**
- N+1 쿼리 문제 방지: IN 절을 활용한 일괄 조회
- 트랜잭션 관리: 여러 Mapper 호출 시 트랜잭션 경계 고려
- 성능 모니터링: 기존 대비 성능 변화 측정

---

## 점진적 마이그레이션 전략

### 5.1 마이그레이션 단계

#### Phase 1: 기반 구조 구축 (1-2주)
**목표**: 새로운 Mapper 구조 준비, 기존 코드 영향 없음

1. **새로운 Mapper 인터페이스 생성**
   - `UserAuthorizationMapper` 생성
   - `ZoneMapper` 생성
   - 기존 `InventoryMapper` 확장

2. **Mapper XML 파일 생성**
   - 각 도메인별 XML 파일 분리
   - 기존 쿼리 복사하여 분리 (아직 사용 안 함)

3. **도메인 모델 클래스 생성**
   - `InventoryInfo`, `ZoneTypeMapping` 등 DTO 클래스 생성

**검증:**
- 컴파일 오류 없음
- 기존 기능 정상 동작 확인

#### Phase 2: 이중화 운영 (2-3주)
**목표**: 신규 구조와 기존 구조 병행 운영

1. **Service 레이어에 신규 메서드 추가**
   - `getSpecificZoneInfoListV2()` 메서드 추가
   - 기존 `getSpecificZoneInfoList()` 유지

2. **Feature Flag 적용**
   ```java
   @Value("${feature.dashboard.v2.enabled:false}")
   private boolean dashboardV2Enabled;
   
   public List<Map<String, Object>> getSpecificZoneInfoList(Map<String, Object> params) {
       if (dashboardV2Enabled) {
           return getSpecificZoneInfoListV2(params);
       }
       return getSpecificZoneInfoListV1(params); // 기존 로직
   }
   ```

3. **모니터링 및 로깅 강화**
   - 신규 메서드 호출 시 로깅
   - 성능 메트릭 수집 (응답 시간, 쿼리 실행 시간)
   - 오류 발생 시 자동 롤백

**검증:**
- 신규 메서드와 기존 메서드 결과 비교
- 성능 비교 (응답 시간, DB 부하)
- 오류율 모니터링

#### Phase 3: 점진적 전환 (2-3주)
**목표**: 사용자 그룹별로 점진적 전환

1. **사용자 그룹별 전환**
   - 내부 테스트 사용자 → 운영담당자 → 관제자 → 전체 사용자
   - 각 그룹별 1주일 모니터링 후 다음 그룹 전환

2. **롤백 계획 수립**
   - Feature Flag로 즉시 롤백 가능
   - 문제 발생 시 자동 롤백 스크립트 준비

3. **성능 모니터링**
   - APM 도구를 통한 실시간 모니터링
   - DB 쿼리 실행 계획 분석
   - 메모리 사용량 모니터링

**검증:**
- 각 사용자 그룹별 오류율 < 0.1%
- 응답 시간 기존 대비 ±10% 이내
- DB 부하 기존 대비 증가 없음

#### Phase 4: 완전 전환 및 정리 (1주)
**목표**: 기존 코드 제거 및 최종 정리

1. **기존 코드 제거**
   - `getSpecificZoneInfoListV1()` 메서드 제거
   - 기존 쿼리 제거 (백업은 유지)
   - Feature Flag 제거

2. **문서화**
   - 리팩토링 내용 문서화
   - 새로운 구조 가이드 작성

**검증:**
- 모든 테스트 통과
- 성능 회귀 테스트 통과

### 5.2 마이그레이션 체크리스트

#### 개발 단계
- [ ] 새로운 Mapper 인터페이스 및 XML 생성
- [ ] 도메인 모델 클래스 생성
- [ ] Service 레이어 신규 메서드 구현
- [ ] 단위 테스트 작성
- [ ] 통합 테스트 작성

#### 배포 전
- [ ] Feature Flag 설정 확인
- [ ] 모니터링 설정 확인
- [ ] 롤백 계획 문서화
- [ ] 성능 기준선(baseline) 측정
- [ ] 스테이징 환경 검증

#### 배포 후
- [ ] 모니터링 대시보드 확인
- [ ] 오류 로그 확인
- [ ] 성능 메트릭 확인
- [ ] 사용자 피드백 수집

---

## 운영 리스크 최소화 방안

### 6.1 기술적 방안

#### 6.1.1 Feature Flag 활용
```java
@Configuration
public class FeatureFlagConfig {
    
    @Bean
    @ConfigurationProperties(prefix = "feature")
    public FeatureFlags featureFlags() {
        return new FeatureFlags();
    }
}

// application.properties
feature.dashboard.v2.enabled=false
feature.dashboard.v2.user-groups=internal-test,admin
```

**장점:**
- 코드 배포와 기능 활성화 분리
- 즉시 롤백 가능 (재배포 불필요)
- 사용자 그룹별 제어 가능

#### 6.1.2 Circuit Breaker 패턴
```java
@Service
public class DashboardServiceImpl {
    
    @CircuitBreaker(name = "dashboard-service", fallbackMethod = "getSpecificZoneInfoListFallback")
    public List<Map<String, Object>> getSpecificZoneInfoListV2(Map<String, Object> params) {
        // 신규 로직
    }
    
    public List<Map<String, Object>> getSpecificZoneInfoListFallback(
            Map<String, Object> params, Exception e) {
        log.warn("Fallback to V1 due to error: {}", e.getMessage());
        return getSpecificZoneInfoListV1(params); // 기존 로직으로 자동 전환
    }
}
```

**장점:**
- 신규 로직 오류 시 자동으로 기존 로직으로 전환
- 시스템 안정성 향상

#### 6.1.3 캐싱 전략
```java
@Service
public class DashboardServiceImpl {
    
    @Cacheable(value = "zoneTypeMapping", key = "#l4CodeList")
    public List<ZoneTypeMapping> getZoneTypeMapping(List<String> l4CodeList) {
        return zoneMapper.selectZoneTypeMappingByL4CodeList(l4CodeList);
    }
    
    @Cacheable(value = "commonCode", key = "#groupCode")
    public List<Map<String, Object>> getCommonCodeList(String groupCode) {
        return commonCodeMapper.selectCommonCodeList(groupCode);
    }
}
```

**장점:**
- DB 부하 감소
- 응답 시간 개선
- 공통코드 같은 변경 빈도가 낮은 데이터에 효과적

#### 6.1.4 배치 처리 최적화
```java
// N+1 쿼리 방지: 개별 조회 대신 일괄 조회
List<String> zoneCodes = inventoryList.stream()
    .map(InventoryInfo::getZone)
    .distinct()
    .collect(Collectors.toList());

// 일괄 조회
List<ZoneTypeMapping> mappings = 
    zoneMapper.selectZoneTypeMappingByL4CodeList(zoneCodes);
```

### 6.2 운영 방안

#### 6.2.1 모니터링 강화
**메트릭 수집:**
- API 응답 시간 (P50, P95, P99)
- DB 쿼리 실행 시간
- 에러율
- 메모리 사용량
- 트랜잭션 수

**알림 설정:**
- 응답 시간 임계값 초과 시 알림
- 에러율 증가 시 알림
- DB 부하 증가 시 알림

#### 6.2.2 로깅 전략
```java
@Slf4j
@Service
public class DashboardServiceImpl {
    
    public List<Map<String, Object>> getSpecificZoneInfoListV2(Map<String, Object> params) {
        String userId = (String) params.get("user_id");
        long startTime = System.currentTimeMillis();
        
        try {
            // 로직 실행
            List<Map<String, Object>> result = ...;
            
            long duration = System.currentTimeMillis() - startTime;
            log.info("getSpecificZoneInfoListV2 completed. userId={}, duration={}ms, resultSize={}", 
                userId, duration, result.size());
            
            return result;
        } catch (Exception e) {
            log.error("getSpecificZoneInfoListV2 failed. userId={}, error={}", 
                userId, e.getMessage(), e);
            throw e;
        }
    }
}
```

#### 6.2.3 롤백 계획
1. **즉시 롤백**: Feature Flag로 비활성화
2. **코드 롤백**: 이전 버전으로 재배포
3. **DB 롤백**: 필요 시 (쿼리 변경이 없는 경우 불필요)

**롤백 트리거 조건:**
- 에러율 > 1%
- 응답 시간 > 기존 대비 2배
- DB 부하 > 기존 대비 1.5배
- 사용자 불만 접수

### 6.3 테스트 전략

#### 6.3.1 단위 테스트
```java
@ExtendWith(MockitoExtension.class)
class DashboardServiceImplTest {
    
    @Mock
    private UserAuthorizationMapper userAuthorizationMapper;
    
    @Mock
    private InventoryMapper inventoryMapper;
    
    @Mock
    private ZoneMapper zoneMapper;
    
    @InjectMocks
    private DashboardServiceImpl dashboardService;
    
    @Test
    void testGetSpecificZoneInfoList() {
        // Given
        when(userAuthorizationMapper.selectAuthorizedZabbixIpList(anyString()))
            .thenReturn(Arrays.asList("192.168.1.1", "192.168.1.2"));
        
        // When
        List<Map<String, Object>> result = 
            dashboardService.getSpecificZoneInfoList(params);
        
        // Then
        assertNotNull(result);
        // 검증 로직
    }
}
```

#### 6.3.2 통합 테스트
- 실제 DB를 사용한 통합 테스트
- 기존 메서드와 신규 메서드 결과 비교 테스트
- 성능 테스트

#### 6.3.3 회귀 테스트
- 기존 기능에 대한 회귀 테스트 스위트 실행
- API 응답 형식 검증

---

## 구현 우선순위

### 7.1 우선순위 기준
1. **영향도**: 많은 사용자가 사용하는 기능 우선
2. **복잡도**: 복잡한 쿼리부터 시작
3. **리스크**: 낮은 리스크부터 시작하여 경험 축적
4. **비즈니스 가치**: 높은 가치를 제공하는 기능 우선

### 7.2 추천 우선순위

#### Phase 1: 낮은 리스크, 높은 학습 가치 (1-2개월)
1. **CommonCode 도메인 분리** (우선순위: 높음)
   - 리스크: 낮음 (캐싱 가능, 변경 빈도 낮음)
   - 학습 가치: 도메인 분리 패턴 학습
   - 예상 효과: 다른 도메인에서 재사용 가능

2. **Zone 도메인 분리** (우선순위: 높음)
   - 리스크: 낮음 (단순 조회)
   - 학습 가치: 매핑 데이터 처리 패턴 학습

#### Phase 2: 중간 리스크, 높은 영향도 (2-3개월)
3. **UserAuthorization 도메인 분리** (우선순위: 높음)
   - 리스크: 중간 (보안 관련)
   - 영향도: 높음 (모든 쿼리에 사용)
   - 예상 효과: 권한 로직 재사용, 보안 강화

4. **Inventory 도메인 확장** (우선순위: 중간)
   - 리스크: 중간
   - 영향도: 높음

#### Phase 3: 높은 복잡도, 높은 가치 (3-4개월)
5. **Dashboard 도메인 리팩토링** (우선순위: 중간)
   - 리스크: 높음 (복잡한 조합 로직)
   - 영향도: 높음
   - 예상 효과: 전체 구조 개선의 모델이 됨

6. **Event 도메인 분리** (우선순위: 중간)
   - 리스크: 중간
   - 영향도: 높음

7. **Incident 도메인 분리** (우선순위: 낮음)
   - 리스크: 중간
   - 영향도: 중간

### 7.3 예상 일정

| Phase | 기간 | 주요 작업 | 리스크 |
|-------|------|----------|--------|
| Phase 1 | 1-2개월 | CommonCode, Zone 분리 | 낮음 |
| Phase 2 | 2-3개월 | UserAuthorization, Inventory 분리 | 중간 |
| Phase 3 | 3-4개월 | Dashboard, Event, Incident 분리 | 중간-높음 |
| **총계** | **6-9개월** | 전체 리팩토링 완료 | - |

---

## 부록

### A. 참고 자료
- Spring Boot 공식 문서
- MyBatis 공식 문서
- 마이크로서비스 패턴 (Martin Fowler)
- 도메인 주도 설계 (DDD)

### B. 용어 정의
- **도메인**: 비즈니스 로직의 단위
- **Mapper**: MyBatis의 데이터 접근 계층
- **Service**: 비즈니스 로직 계층
- **Feature Flag**: 기능 활성화/비활성화를 제어하는 플래그

### C. 변경 이력
- 2024-XX-XX: 초안 작성

---

## 결론

이 리팩토링 초안은 **운영 중인 시스템의 안정성을 최우선**으로 고려하여 작성되었습니다. 

**핵심 전략:**
1. 점진적 마이그레이션으로 리스크 최소화
2. Feature Flag를 통한 즉시 롤백 가능
3. 이중화 운영을 통한 검증
4. 강화된 모니터링과 알림

**예상 효과:**
- 코드 재사용성 향상
- 유지보수성 개선
- 테스트 용이성 향상
- 향후 마이크로서비스 전환 기반 마련

**다음 단계:**
1. 이 문서에 대한 검토 및 피드백 수집
2. Phase 1 작업 계획 수립
3. 팀 내 공유 및 승인
4. 프로토타입 개발 및 검증


